tarefa01Thread01.controller;
public class ThreadId extends Thread {
	private int idThread;
	public ThreadId(int idThread) {
		this.idThread = idThread;
	}
	public void run() {
		System.out.println("ThreadId#" + idThread);
	}
}
